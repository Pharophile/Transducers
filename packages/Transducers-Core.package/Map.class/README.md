A Map transducer builds a reducing function that evaluates a given reducing function with the images of elements as values under a mapping.

Instance Variables
	function	<Block | Symbol>	a mapping function that maps elements
